紅石計數顯示器（v2 壓縮版）｜Minecraft 基岩版紅石機器

【v2 更新】
・修正數字左右顛倒：從顯示器正前方看，1～9 都是正向。
・電路重新設計壓縮：體積由 40×42×12 縮小到 17×25×12（約原本的 1/4）。
  七段解碼改為串接式、兩段共用一條刻度線；重置改為免閂鎖設計；按鈕與計數核心移到顯示器旁。

【這是什麼】
一台用純紅石做的數字顯示器（七段顯示，紅石燈）：
  ・按「+1」按鈕：數字加一。
  ・加到 9 就停住，再按也不會變。
  ・按「重置」按鈕：數字回到 1（會看到 9→8→…→1 快速倒數約 3～4 秒）。
沒有使用命令方塊，全部是紅石粉、中繼器、比較器、火把、投擲器與漏斗。

【檔案】
紅石計數顯示器.mcpack          行為包（內含結構）
紅石計數顯示器_使用說明.txt    本說明
紅石計數顯示器_整體渲染.png    整體斜俯視＋數字 1～9 的模擬顯示
紅石計數顯示器_內部剖視.png    三層電路平面圖與原理
兩張圖依方塊資料和內建紅石模擬器繪製，不是遊戲截圖。

【尺寸】
寬 17 × 深 25 × 高 12 格（結構方塊 X=17、Y=12、Z=25）。
顯示器在結構北側，面向北（-Z）；玩家站在結構北邊（外面）面向顯示器看。

【匯入與放置】
1. 雙擊「紅石計數顯示器.mcpack」，用 Minecraft 匯入。
2. 世界設定 → 行為包 → 啟用「紅石計數顯示器」。
3. 開啟作弊（允許指令），輸入：/give @s structure_block
4. 放下結構方塊，模式切成「載入」，結構名稱輸入：redcount_260924:counter_1to9
5. 旋轉 0°、不鏡像，按載入。
   或直接輸入指令：/structure load redcount_260924:counter_1to9 ~ ~ ~
   （結構會以你所在位置為最小角往 +X、+Y、+Z 展開，請先站到空地的西北角。）
6. 請預留 17×25×12 格平地；結構內的空氣會清掉原有方塊。
   結構最底層 Y=0 就是地板，放在平地上即可。

【操作位置（相對結構原點）】
  ・「+1」按鈕（石頭按鈕）：X=15 Y=2 Z=0，控制台左邊（面向顯示器時）。
  ・「重置」按鈕（黑石按鈕）：X=11 Y=2 Z=0，控制台右邊（面向顯示器時）。
  控制台在顯示器左手邊（面向顯示器時），上面有告示牌標示。
  載入後顯示「1」。

【運作原理（簡述）】
1. 記憶：投擲器 C 裡放幾把木劍就代表數字（n 把 → 顯示 n+1）。
   比較器讀 C 的滿度，得到訊號強度 0/2/4/5/7/8/10/11/13。
2. +1：按鈕讓投擲器 R 丟一把木劍往上進漏斗，再進 C。
   整台機器只有 8 把木劍，R 丟完就不能再加 → 自然停在 9。
3. 重置：按鈕讓 C 下方的漏斗 H 解鎖，開始把木劍抽回 R；
   另一個比較器讀 H，只要 H 裡有木劍就維持解鎖（再加約 1.2 秒延時），
   C 抽空後 H 自動重新上鎖。
4. 解碼：訊號經比較器匯流排送到 4 條紅石粉「衰減刻度線」（每格少 1），
   每條線左右各接一段（a～g 共 7 段）。中繼器取出「數字≥k」，減法比較器做「A 且非 B」，
   併入下一格做「或」，一欄串到底就是一段的訊號。
   Y2 平面做 c/d/e，Y5 平面做 b/g/f/a（類比升降梯無損送上去），a 再用火把塔升到 Y8。
5. 顯示：橫段（a/g/d）用兩個中繼器直接點燈；直段（b/c/e/f）用一支紅石火把點下燈、
   同時強充能上方方塊點上燈，所以直段模組輸出的是「反相」訊號。

【注意事項】
・計數核心（兩個投擲器、兩個漏斗，位於顯示器左後方、有告示牌）內的 8 把木劍是機器的一部分，
  請不要取出或放入東西，否則數字會錯亂。如果弄亂了：把 C、H、Hs 清空，R 裡放剛好 8 把木劍即可恢復。
・連按很快沒關係；木劍經過漏斗需要約 0.4 秒，顯示會稍微延遲跟上。
・重置倒數時請等它回到 1 再按 +1（中途按也不會壞，只是會被一起抽回）。
・請勿用活塞推動或破壞電路；電路層在 Y1～Y8，四周開放方便觀察。

【測試狀態】
・已重新解析 NBT、檢查尺寸、方塊名稱與狀態（Mojang 官方 1.26.50 方塊表）、包內結構一致與 ZIP CRC。
・生成器內建紅石模擬器：已逐一驗證 C 內 0～8 把時燈號正確顯示 1～9，
  以及「按重置 → 漏斗解鎖 → 逐把抽回 → 抽空自動上鎖」與「+1 只觸發 R、不觸發 C」，
  並以玩家視角輸出 1～9 的燈號確認不再左右顛倒。
・重置依賴「漏斗推出與抽入之間的空檔小於延時（約 1.2 秒）」這個時序假設；
  若遊戲中重置停在半途，再按一次重置即可繼續。
・中繼器／比較器方向依 minecraft.wiki 基岩版方塊狀態定義設定。
・尚未在 Minecraft 客戶端實際載入測試。模擬器是依 Java/基岩共通規則寫的簡化模型，
  不含更新順序與時序細節；若遊戲內某一段顯示不對，多半是單一元件的方向或更新問題，
  可對照「內部剖視」圖檢查。
結構 SHA-256：23af0a47671e882350605a690bd8e02fcecb1b99eb92ee73656cabcbfa018a62
